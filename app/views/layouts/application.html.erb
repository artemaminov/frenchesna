<!DOCTYPE html>
<html>
  <head>
    <title><%= t('content.breed') %> Frenchesna</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= render 'shared/yandex-metrika' %>
  </head>

  <body style="background-image: url(<%= crop_image(@background, :background) unless @background.blank? %>)">
    <header>
      <div class="logo">
        <a href="/"></a>
      </div>
      <div class="contacts">
        <a class="phone" href="tel:+79772614163">+7 (977) 261-41-63</a><br>
        <span class="address"><%= t('content.address') %></span>
      </div>
      <div class="language__switcher">
        <a href="?locale=ru" class="ru"><%= svg_tag("ru", class: "icon") %></a>
        <a href="?locale=en" class="en"><%= svg_tag("en", class: "icon") %></a>
      </div>
    </header>
    <div class="content">
      <div class="sidebar left">
        <div class="menu">
          <input type="checkbox" id="burger">
          <div class="burger-menu">
            <label for="burger">
              <span class="line top"></span>
              <span class="line mid-top"></span>
              <span class="line mid-bottom"></span>
              <span class="line bottom"></span>
            </label>
          </div>
          <%= render partial: "shared/menu", locals: { dogs: @dogs, puppies: @puppies, current_dog: @dog } %>
        </div>
      </div>
      <div class="main">
        <%= yield %>
      </div>
      <div class="sidebar right">
        <%= yield :right_sidebar %>
      </div>
    </div>
    <footer>
      <div class="contacts">
        <span class="address"><%= t('content.address') %></span>
        <br>
        <a class="phone" href="tel:+79772614163">+7 (977) 261-41-63</a>
      </div>
      <div class="logo">
        <a href="/"></a>
      </div>
    </footer>
  </body>
</html>
